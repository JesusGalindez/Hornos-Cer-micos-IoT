{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKER",
    "dockerfilePath": "Dockerfile",
    "context": "backend"
  },
  "deploy": {
    "numReplicas": 1,
    "sleepApplication": false
  }
}
